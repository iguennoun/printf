#include "main.h"

/**
** _printf - <stdio.h> 'printf' lookalike.
** @format: The format string is a character string,
**          beginning and ending in its initial shift state, if any.
**
** Return: The number of characters that are printed.
**         If error then it returns a negative value.
*/
int _printf(const char *format, ...)
{
	int nbrchar_printed = 0;

	nbrchar_printed = 1;

	return (0);
}
